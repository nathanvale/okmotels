schema: http://localhost:8000/___graphql
documents: 'src/**/*.tsx'
generates:
  ./src/graphql.d.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      enumsAsTypes: true
      immutableTypes: true
      skipTypename: true